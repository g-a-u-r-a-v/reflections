What happens when you initialize a repository? Why do you need to do it?
When we initialize a repository git makes some hidden files with 
.git extension and it track all files withinn that directory.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
staging area is that place which required to be commited.
we can run git commit command to add the changes.